/* The system breakpoints' JS mirror (step 11.1). The CSS source of
   truth is src/styles/tokens/breakpoints.css; test/breakpoints.test.ts
   pins the two equal, so a retune edits both or fails the suite. */
export const BP_PHONE_MAX = 768;
