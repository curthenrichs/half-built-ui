---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["two-column", className]}>
  <main class="site-main"><slot /></main>
  <slot name="aside" />
</div>
