---
interface Props {
  size?: number;
  class?: string;
}

const { size = 40, class: className } = Astro.props;
---

<div
  aria-hidden="true"
  style={`height:${size}px`}
  class:list={["spacer", className]}
></div>
